# Postgres connection string (Render free Postgres, or local for dev)
DATABASE_URL=postgres://user:password@localhost:5432/cellbodcast

# Shared secret required in the X-Admin-Key header to POST /alerts and to log
# into the /admin page. Set a real random value in Render's dashboard.
ADMIN_KEY=change-me

PORT=3000
